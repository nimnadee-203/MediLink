# MediLink admin (doctor dashboard). Copy to `.env` for Vite.

# If you run the API gateway (recommended: one port for patient + doctor + appointments):
# VITE_BACKEND_URL=http://localhost:8000

# If you talk to doctor-service only (no gateway):
# VITE_BACKEND_URL=http://localhost:4000
